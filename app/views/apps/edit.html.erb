<div class="row center-block">
  <h2>Edit Block</h2>

  <%= form_for(:app, :url => {:action => 'update',:id => @app.permalink },html:{class: "form-horizontal"}) do |f| %>

      <%= render(:partial => "form", :locals => {:f => f}) %>

      <div class="form-buttons dm">
        <%= submit_tag("Create Block ") %>
      </div>

  <% end %>
</div>